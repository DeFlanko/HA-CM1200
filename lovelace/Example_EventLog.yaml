type: markdown
title: CM1200 Event Log
content: >
  <pre>

  Time                       Priority     Description

  {% for entry in (state_attr('sensor.cm1200_cable_modem_cm1200_event_log',
  'entries') or [])[:50] -%}

  {{ entry.time.ljust(25) }}  {{ entry.priority.ljust(12) }} {{
  entry.description }}

  {% endfor %}

  </pre>
grid_options:
  columns: 48
  rows: auto
